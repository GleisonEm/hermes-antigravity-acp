diff --git a/agent/agent_init.py b/agent/agent_init.py
index 797c18c6df..ff4cb1b077 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -725,8 +725,8 @@ def init_agent(
     if (
         api_mode is None
         and agent.api_mode == "chat_completions"
-        and agent.provider != "copilot-acp"
-        and not str(agent.base_url or "").lower().startswith("acp://copilot")
+        and agent.provider not in {"copilot-acp", "antigravity-acp"}
+        and not str(agent.base_url or "").lower().startswith("acp://")
         and not str(agent.base_url or "").lower().startswith("acp+tcp://")
         and not agent._is_azure_openai_url()
         and (
@@ -1188,7 +1188,7 @@ def init_agent(
                 client_kwargs = {"api_key": api_key, "base_url": base_url}
             if _provider_timeout is not None:
                 client_kwargs["timeout"] = _provider_timeout
-            if agent.provider == "copilot-acp":
+            if agent.provider in {"copilot-acp", "antigravity-acp"}:
                 client_kwargs["command"] = agent.acp_command
                 client_kwargs["args"] = agent.acp_args
             effective_base = base_url
diff --git a/agent/agent_runtime_helpers.py b/agent/agent_runtime_helpers.py
index 50db73c3b3..879e51681e 100644
--- a/agent/agent_runtime_helpers.py
+++ b/agent/agent_runtime_helpers.py
@@ -2281,7 +2281,9 @@ def create_openai_client(agent, client_kwargs: dict, *, reason: str, shared: boo
     httpx_verify = resolve_httpx_verify(ca_bundle=ssl_ca_cert, ssl_verify=ssl_verify_cfg)
     _validate_proxy_env_urls()
     _validate_base_url(client_kwargs.get("base_url"))
-    if agent.provider == "copilot-acp" or str(client_kwargs.get("base_url", "")).startswith("acp://copilot"):
+    if agent.provider in {"copilot-acp", "antigravity-acp"} or str(
+        client_kwargs.get("base_url", "")
+    ).startswith(("acp://copilot", "acp://antigravity")):
         from agent.copilot_acp_client import CopilotACPClient
 
         client = CopilotACPClient(**client_kwargs)
@@ -2623,6 +2625,25 @@ def switch_model(agent, new_model, new_provider, api_key='', base_url='', api_mo
             _sm_timeout = get_provider_request_timeout(agent.provider, agent.model)
             if _sm_timeout is not None:
                 agent._client_kwargs["timeout"] = _sm_timeout
+            # ACP subprocess providers (copilot-acp, antigravity-acp) need
+            # their kernel/CLI command on EVERY client rebuild. switch_model()
+            # receives creds without command/args, so re-resolve here — same
+            # call the runtime uses at session start. Without this, a
+            # mid-session /model switch to an ACP provider builds the client
+            # without command and falls back to `copilot` (#local-fix).
+            if new_provider in {"copilot-acp", "antigravity-acp"}:
+                try:
+                    from hermes_cli.auth import (
+                        resolve_external_process_provider_credentials,
+                    )
+
+                    _acp_creds = resolve_external_process_provider_credentials(new_provider)
+                    agent.acp_command = _acp_creds.get("command") or ""
+                    agent.acp_args = list(_acp_creds.get("args") or [])
+                except Exception:
+                    pass
+                agent._client_kwargs["command"] = getattr(agent, "acp_command", None)
+                agent._client_kwargs["args"] = list(getattr(agent, "acp_args", []) or [])
             # Reapply provider-specific headers (e.g. OpenRouter HTTP-Referer,
             # X-Title) that were lost when _client_kwargs was rebuilt from
             # scratch.  Without this, model switches clear attribution headers
diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index 99da9ee2f7..5f0a06acad 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -6633,21 +6633,21 @@ def resolve_provider_client(
             or _read_main_model_for_aux(),
             provider,
         )
-        if provider == "copilot-acp":
+        if provider in {"copilot-acp", "antigravity-acp"}:
             api_key = str(creds.get("api_key", "")).strip()
             base_url = str(creds.get("base_url", "")).strip()
             command = str(creds.get("command", "")).strip() or None
             args = list(creds.get("args") or [])
             if not final_model:
                 logger.warning(
-                    "resolve_provider_client: copilot-acp requested but no model "
-                    "was provided or configured"
+                    "resolve_provider_client: %s requested but no model "
+                    "was provided or configured", provider
                 )
                 return None, None
             if not api_key or not base_url:
                 logger.warning(
-                    "resolve_provider_client: copilot-acp requested but external "
-                    "process credentials are incomplete"
+                    "resolve_provider_client: %s requested but external "
+                    "process credentials are incomplete", provider
                 )
                 return None, None
             from agent.copilot_acp_client import CopilotACPClient
diff --git a/agent/chat_completion_helpers.py b/agent/chat_completion_helpers.py
index 6a87c7c1e0..6f17c73239 100644
--- a/agent/chat_completion_helpers.py
+++ b/agent/chat_completion_helpers.py
@@ -2197,8 +2197,19 @@ def try_activate_fallback(agent, reason: "FailoverReason | None" = None) -> bool
             agent._client_kwargs = {
                 "api_key": fb_client.api_key,
                 "base_url": fb_base_url,
-                **({"default_headers": dict(fb_headers)} if fb_headers else {}),
+                **(dict(fb_headers) if fb_headers else {}),
             }
+            if fb_provider in {"copilot-acp", "antigravity-acp"}:
+                # ACP subprocess providers: carry the kernel/CLI command on
+                # every client rebuild. fb_client is a CopilotACPClient here
+                # (built by resolve_provider_client) — copy its resolved
+                # command/args so subsequent request rebuilds work.
+                agent._client_kwargs["command"] = getattr(
+                    fb_client, "_acp_command", None
+                )
+                agent._client_kwargs["args"] = list(
+                    getattr(fb_client, "_acp_args", None) or []
+                )
             if _fb_timeout is not None:
                 agent._client_kwargs["timeout"] = _fb_timeout
                 # Rebuild the shared OpenAI client so the configured
diff --git a/agent/conversation_loop.py b/agent/conversation_loop.py
index 1a236e6eac..bc5479726a 100644
--- a/agent/conversation_loop.py
+++ b/agent/conversation_loop.py
@@ -2652,8 +2652,8 @@ def run_conversation(
                 # stream.  Mirror the ACP exclusion used for Responses
                 # API upgrade (lines ~1083-1085).
                 elif (
-                    agent.provider in {"copilot-acp"}
-                    or str(agent.base_url or "").lower().startswith("acp://copilot")
+                    agent.provider in {"copilot-acp", "antigravity-acp"}
+                    or str(agent.base_url or "").lower().startswith("acp://")
                     or str(agent.base_url or "").lower().startswith("acp+tcp://")
                 ):
                     _use_streaming = False
diff --git a/agent/copilot_acp_client.py b/agent/copilot_acp_client.py
index 9cbdcd3494..724a7b2dc2 100644
--- a/agent/copilot_acp_client.py
+++ b/agent/copilot_acp_client.py
@@ -9,6 +9,7 @@ back into the minimal shape Hermes expects from an OpenAI client.
 from __future__ import annotations
 
 import json
+import logging
 import os
 import queue
 import re
@@ -27,6 +28,8 @@ from openai.types.chat.chat_completion_message_tool_call import (
 )
 
 from agent.file_safety import get_read_block_error, get_write_denied_error
+
+logger = logging.getLogger(__name__)
 from agent.redact import redact_sensitive_text
 from tools.environments.local import hermes_subprocess_env
 
@@ -413,8 +416,13 @@ class CopilotACPClient:
         self.base_url = base_url or ACP_MARKER_BASE_URL
         self._default_headers = dict(default_headers or {})
         self._acp_command = acp_command or command or _resolve_command()
-        self._acp_args = list(acp_args or args or _resolve_args())
+        self._acp_args = list(
+            acp_args
+            if acp_args is not None
+            else (args if args is not None else _resolve_args())
+        )
         self._acp_cwd = str(Path(acp_cwd or os.getcwd()).resolve())
+        self._kernel_model_ids: set[str] = set()
         self.chat = _ACPChatNamespace(self)
         self.is_closed = False
         self._active_process: subprocess.Popen[str] | None = None
@@ -473,6 +481,7 @@ class CopilotACPClient:
         response_text, reasoning_text = self._run_prompt(
             prompt_text,
             timeout_seconds=_effective_timeout,
+            model=model,
         )
 
         tool_calls, cleaned_text = _extract_tool_calls_from_text(response_text)
@@ -501,7 +510,9 @@ class CopilotACPClient:
             return _completion_to_stream_chunks(completion)
         return completion
 
-    def _run_prompt(self, prompt_text: str, *, timeout_seconds: float) -> tuple[str, str]:
+    def _run_prompt(
+        self, prompt_text: str, *, timeout_seconds: float, model: str | None = None
+    ) -> tuple[str, str]:
         try:
             # Hide the console the CLI child would otherwise flash on Windows
             # (#56747). Hide-only — stdio pipes stay intact for the ACP wire.
@@ -646,6 +657,32 @@ class CopilotACPClient:
             if not session_id:
                 raise RuntimeError("Copilot ACP did not return a sessionId.")
 
+            # Google Antigravity ACP kernel: honor real model slugs by setting
+            # the session's "model" config option (ACP session/set_config_option).
+            # Failures are non-fatal — the kernel keeps its default model.
+            if str(self.base_url or "").startswith("acp://antigravity"):
+                try:
+                    kernel_models = ((session.get("models") or {}).get("availableModels")) or []
+                    self._kernel_model_ids = {
+                        str(m.get("modelId") or "").strip()
+                        for m in kernel_models
+                        if isinstance(m, dict)
+                    }
+                    if model and model in self._kernel_model_ids:
+                        _request(
+                            "session/set_config_option",
+                            {
+                                "sessionId": session_id,
+                                "configId": "model",
+                                "value": model,
+                            },
+                        )
+                        logger.info(
+                            "Antigravity ACP kernel: session model set to %s", model
+                        )
+                except Exception:
+                    self._kernel_model_ids = set()
+
             text_parts: list[str] = []
             reasoning_parts: list[str] = []
             _request(
diff --git a/hermes_cli/auth.py b/hermes_cli/auth.py
index 0ecd267513..e820eb787d 100644
--- a/hermes_cli/auth.py
+++ b/hermes_cli/auth.py
@@ -134,6 +134,7 @@ MINIMAX_OAUTH_REFRESH_SKEW_SECONDS = 60
 DEFAULT_QWEN_BASE_URL = "https://portal.qwen.ai/v1"
 DEFAULT_GITHUB_MODELS_BASE_URL = "https://api.githubcopilot.com"
 DEFAULT_COPILOT_ACP_BASE_URL = "acp://copilot"
+DEFAULT_ANTIGRAVITY_ACP_BASE_URL = "acp://antigravity"
 DEFAULT_OLLAMA_CLOUD_BASE_URL = "https://ollama.com/v1"
 DEFAULT_ACTUAL_BASE_URL = "https://api.actual.inc/v1"
 DEFAULT_ACTUAL_LOCAL_BASE_URL = "http://127.0.0.1:8080/v1"
@@ -305,6 +306,13 @@ PROVIDER_REGISTRY: Dict[str, ProviderConfig] = {
         inference_base_url=DEFAULT_COPILOT_ACP_BASE_URL,
         base_url_env_var="COPILOT_ACP_BASE_URL",
     ),
+    "antigravity-acp": ProviderConfig(
+        id="antigravity-acp",
+        name="Google Antigravity ACP",
+        auth_type="external_process",
+        inference_base_url=DEFAULT_ANTIGRAVITY_ACP_BASE_URL,
+        base_url_env_var="ANTIGRAVITY_ACP_BASE_URL",
+    ),
     "gemini": ProviderConfig(
         id="gemini",
         name="Google AI Studio",
@@ -2058,6 +2066,8 @@ def resolve_provider(
         "github": "copilot", "github-copilot": "copilot",
         "github-models": "copilot", "github-model": "copilot",
         "github-copilot-acp": "copilot-acp", "copilot-acp-agent": "copilot-acp",
+        "antigravity": "antigravity-acp", "antigravity-acp": "antigravity-acp",
+        "agy-acp": "antigravity-acp", "antigravity-acp-agent": "antigravity-acp",
         "aigateway": "ai-gateway", "vercel": "ai-gateway", "vercel-ai-gateway": "ai-gateway",
         "opencode": "opencode-zen", "zen": "opencode-zen",
         "qwen-portal": "qwen-oauth", "qwen-cli": "qwen-oauth", "qwen-oauth": "qwen-oauth",
@@ -7038,19 +7048,60 @@ def get_api_key_provider_status(provider_id: str) -> Dict[str, Any]:
     }
 
 
+def _default_antigravity_acp_command() -> str:
+    """Locate the Antigravity ACP kernel (agy_acp_server.par).
+
+    Search order:
+      1. Zed's Antigravity extension registry copy (newest versioned dir) —
+         preferred because the kernel finds its ``localharness_external``
+         sandbox binary relative to its REAL launch path; launching through
+         a symlink (e.g. ~/.local/bin/agy_acp_server.par) makes session/new
+         fail with "Could not find default localharness binary".
+      2. ~/.local/bin/agy_acp_server.par  (stable user location / symlink)
+    """
+    candidates: list[str] = []
+    try:
+        zed_registry = (
+            Path.home()
+            / "Library/Application Support/Zed/external_agents/registry/antigravity-acp"
+        )
+        if zed_registry.is_dir():
+            for child in sorted(zed_registry.iterdir(), reverse=True):
+                par = child / "agy_acp_server.par"
+                if par.is_file():
+                    candidates.append(str(par))
+                    break
+    except Exception:
+        pass
+    candidates.append(os.path.expanduser("~/.local/bin/agy_acp_server.par"))
+    for candidate in candidates:
+        if candidate and os.path.isfile(candidate) and os.access(candidate, os.X_OK):
+            return candidate
+    return candidates[0] if candidates else "agy_acp_server.par"
+
+
 def get_external_process_provider_status(provider_id: str) -> Dict[str, Any]:
     """Status snapshot for providers that run a local subprocess."""
     pconfig = PROVIDER_REGISTRY.get(provider_id)
     if not pconfig or pconfig.auth_type != "external_process":
         return {"configured": False}
 
-    command = (
-        os.getenv("HERMES_COPILOT_ACP_COMMAND", "").strip()
-        or os.getenv("COPILOT_CLI_PATH", "").strip()
-        or "copilot"
-    )
-    raw_args = os.getenv("HERMES_COPILOT_ACP_ARGS", "").strip()
-    args = shlex.split(raw_args) if raw_args else ["--acp", "--stdio"]
+    if provider_id == "antigravity-acp":
+        command = (
+            os.getenv("HERMES_ANTIGRAVITY_ACP_COMMAND", "").strip()
+            or os.getenv("AGY_ACP_COMMAND", "").strip()
+            or _default_antigravity_acp_command()
+        )
+        raw_args = os.getenv("HERMES_ANTIGRAVITY_ACP_ARGS", "").strip()
+        args = shlex.split(raw_args) if raw_args else []
+    else:
+        command = (
+            os.getenv("HERMES_COPILOT_ACP_COMMAND", "").strip()
+            or os.getenv("COPILOT_CLI_PATH", "").strip()
+            or "copilot"
+        )
+        raw_args = os.getenv("HERMES_COPILOT_ACP_ARGS", "").strip()
+        args = shlex.split(raw_args) if raw_args else ["--acp", "--stdio"]
     base_url = os.getenv(pconfig.base_url_env_var, "").strip() if pconfig.base_url_env_var else ""
     if not base_url:
         base_url = pconfig.inference_base_url
@@ -7087,6 +7138,8 @@ def get_auth_status(provider_id: Optional[str] = None) -> Dict[str, Any]:
         return get_minimax_oauth_auth_status()
     if target == "copilot-acp":
         return get_external_process_provider_status(target)
+    if target == "antigravity-acp":
+        return get_external_process_provider_status(target)
     if target == "azure-foundry":
         return _get_azure_foundry_auth_status()
     # API-key providers
@@ -7275,15 +7328,32 @@ def resolve_external_process_provider_credentials(provider_id: str) -> Dict[str,
     if not base_url:
         base_url = pconfig.inference_base_url
 
-    command = (
-        os.getenv("HERMES_COPILOT_ACP_COMMAND", "").strip()
-        or os.getenv("COPILOT_CLI_PATH", "").strip()
-        or "copilot"
-    )
-    raw_args = os.getenv("HERMES_COPILOT_ACP_ARGS", "").strip()
-    args = shlex.split(raw_args) if raw_args else ["--acp", "--stdio"]
+    if provider_id == "antigravity-acp":
+        command = (
+            os.getenv("HERMES_ANTIGRAVITY_ACP_COMMAND", "").strip()
+            or os.getenv("AGY_ACP_COMMAND", "").strip()
+            or _default_antigravity_acp_command()
+        )
+        raw_args = os.getenv("HERMES_ANTIGRAVITY_ACP_ARGS", "").strip()
+        args = shlex.split(raw_args) if raw_args else []
+    else:
+        command = (
+            os.getenv("HERMES_COPILOT_ACP_COMMAND", "").strip()
+            or os.getenv("COPILOT_CLI_PATH", "").strip()
+            or "copilot"
+        )
+        raw_args = os.getenv("HERMES_COPILOT_ACP_ARGS", "").strip()
+        args = shlex.split(raw_args) if raw_args else ["--acp", "--stdio"]
     resolved_command = shutil.which(command) if command else None
     if not resolved_command and not base_url.startswith("acp+tcp://"):
+        if provider_id == "antigravity-acp":
+            raise AuthError(
+                f"Could not find the Antigravity ACP kernel '{command}'. "
+                "Install it with the Zed Antigravity extension or set "
+                "HERMES_ANTIGRAVITY_ACP_COMMAND/AGY_ACP_COMMAND to its path.",
+                provider=provider_id,
+                code="missing_antigravity_acp_kernel",
+            )
         raise AuthError(
             f"Could not find the Copilot CLI command '{command}'. "
             "Install GitHub Copilot CLI or set HERMES_COPILOT_ACP_COMMAND/COPILOT_CLI_PATH.",
@@ -7291,9 +7361,10 @@ def resolve_external_process_provider_credentials(provider_id: str) -> Dict[str,
             code="missing_copilot_cli",
         )
 
+    api_key = "copilot-acp" if provider_id == "copilot-acp" else provider_id
     return {
         "provider": provider_id,
-        "api_key": "copilot-acp",
+        "api_key": api_key,
         "base_url": base_url.rstrip("/"),
         "command": resolved_command or command,
         "args": args,
diff --git a/hermes_cli/main.py b/hermes_cli/main.py
index e81b57a493..22fdaa6f50 100644
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -795,6 +795,7 @@ from hermes_cli.model_setup_flows import (
     _model_flow_named_custom,
     _model_flow_copilot,
     _model_flow_copilot_acp,
+    _model_flow_antigravity_acp,
     _model_flow_kimi,
     _model_flow_stepfun,
     _model_flow_bedrock_api_key,
@@ -3480,6 +3481,8 @@ def select_provider_and_model(args=None):
         _model_flow_minimax_oauth(config, current_model, args=args)
     elif selected_provider == "copilot-acp":
         _model_flow_copilot_acp(config, current_model)
+    elif selected_provider == "antigravity-acp":
+        _model_flow_antigravity_acp(config, current_model)
     elif selected_provider == "copilot":
         _model_flow_copilot(config, current_model)
     elif selected_provider == "custom":
diff --git a/hermes_cli/model_setup_flows.py b/hermes_cli/model_setup_flows.py
index f62b78faac..b8c3e99d71 100644
--- a/hermes_cli/model_setup_flows.py
+++ b/hermes_cli/model_setup_flows.py
@@ -2023,12 +2023,89 @@ def _model_flow_copilot_acp(config, current_model=""):
 
     print(f"Default model set to: {selected} (via {pconfig.name})")
 
+
+def _model_flow_antigravity_acp(config, current_model=""):
+    """Google Antigravity ACP flow using the local ACP kernel."""
+    from hermes_cli.auth import (
+        PROVIDER_REGISTRY,
+        _prompt_model_selection,
+        _save_model_choice,
+        deactivate_provider,
+        get_external_process_provider_status,
+        resolve_external_process_provider_credentials,
+    )
+    from hermes_cli.config import clear_model_endpoint_credentials, load_config, save_config
+    from hermes_cli.models import _PROVIDER_MODELS
+
+    del config
+
+    provider_id = "antigravity-acp"
+    pconfig = PROVIDER_REGISTRY[provider_id]
+
+    status = get_external_process_provider_status(provider_id)
+    resolved_command = (
+        status.get("resolved_command") or status.get("command") or "agy_acp_server.par"
+    )
+    effective_base = status.get("base_url") or pconfig.inference_base_url
+
+    print("  Google Antigravity ACP delegates Hermes turns to the Antigravity")
+    print("  ACP kernel (agy_acp_server.par).")
+    print("  Hermes currently starts its own ACP subprocess for each request.")
+    print("  Hermes uses your selected model as a hint for the ACP session.")
+    print(f"  Command: {resolved_command}")
+    print(f"  Backend marker: {effective_base}")
+    print()
+
+    try:
+        creds = resolve_external_process_provider_credentials(provider_id)
+    except Exception as exc:
+        print(f"  ⚠ {exc}")
+        print(
+            "  Install it via the Zed Antigravity extension or set "
+            "HERMES_ANTIGRAVITY_ACP_COMMAND/AGY_ACP_COMMAND to its path."
+        )
+        return
+
+    effective_base = creds.get("base_url") or effective_base
+
+    model_list = list(_PROVIDER_MODELS.get("antigravity-acp", ["antigravity-acp"]))
+    if model_list:
+        selected = _prompt_model_selection(
+            model_list,
+            current_model=current_model or model_list[0],
+            confirm_provider=provider_id,
+            confirm_base_url=effective_base,
+        )
+    else:
+        try:
+            selected = input("Model name: ").strip()
+        except (KeyboardInterrupt, EOFError):
+            selected = None
+
+    if not selected:
+        print("No change.")
+        return
+
+    _save_model_choice(selected)
+
+    cfg = load_config()
+    model = cfg.get("model")
+    if not isinstance(model, dict):
+        model = {"default": model} if model else {}
+        cfg["model"] = model
+    model["provider"] = provider_id
+    model["base_url"] = effective_base
+    model["api_mode"] = "chat_completions"
+    clear_model_endpoint_credentials(model, clear_api_mode=False)
+    save_config(cfg)
+    deactivate_provider()
+
+    print(f"Default model set to: {selected} (via {pconfig.name})")
+
+
 def _model_flow_kimi(config, current_model=""):
     """Kimi / Moonshot model selection with automatic endpoint routing.
 
-    - sk-kimi-* keys   → api.kimi.com/coding/v1  (Kimi Coding Plan)
-    - Other keys        → api.moonshot.ai/v1      (legacy Moonshot)
-
     No manual base URL prompt — endpoint is determined by key prefix.
     """
     from hermes_cli.main import _prompt_api_key
diff --git a/hermes_cli/model_switch.py b/hermes_cli/model_switch.py
index aea8efba48..8b02dbda5a 100644
--- a/hermes_cli/model_switch.py
+++ b/hermes_cli/model_switch.py
@@ -2425,6 +2425,16 @@ def list_authenticated_providers(
                 logger.debug("Vertex credential check failed: %s", exc)
         elif overlay.extra_env_vars:
             has_creds = any(os.environ.get(ev) for ev in overlay.extra_env_vars)
+        elif overlay.auth_type == "external_process":
+            # ACP subprocess providers (copilot-acp, antigravity-acp) are
+            # "configured" when their local CLI/kernel binary resolves.
+            try:
+                from hermes_cli.auth import get_external_process_provider_status
+                has_creds = bool(
+                    (get_external_process_provider_status(hermes_slug) or {}).get("configured")
+                )
+            except Exception:
+                has_creds = False
         # Also check api_key_env_vars from PROVIDER_REGISTRY for api_key auth_type
         if not has_creds and overlay.auth_type == "api_key":
             for _key in (pid, hermes_slug):
@@ -2493,7 +2503,7 @@ def list_authenticated_providers(
         if not has_creds:
             continue
 
-        if hermes_slug in {"openai-codex", "copilot", "copilot-acp"}:
+        if hermes_slug in {"openai-codex", "copilot", "copilot-acp", "antigravity-acp"}:
             # Use live OAuth-backed discovery so the gateway /model picker
             # matches what the user's authenticated Codex/Copilot backend
             # actually serves — including ChatGPT-Pro-only Codex slugs
@@ -2618,6 +2628,21 @@ def list_authenticated_providers(
         if not _cp_has_creds and _cp_config and getattr(_cp_config, "auth_type", "") == "aws_sdk":
             _cp_has_creds = _has_aws_sdk_creds_for_listing(_cp.slug)
 
+        # Special case: external_process auth (copilot-acp, antigravity-acp)
+        # — configured when the local ACP CLI/kernel binary resolves.
+        if (
+            not _cp_has_creds
+            and _cp_config
+            and getattr(_cp_config, "auth_type", "") == "external_process"
+        ):
+            try:
+                from hermes_cli.auth import get_external_process_provider_status
+                _cp_has_creds = bool(
+                    (get_external_process_provider_status(_cp.slug) or {}).get("configured")
+                )
+            except Exception:
+                pass
+
         if not _cp_has_creds:
             continue
 
diff --git a/hermes_cli/models.py b/hermes_cli/models.py
index cc44fc44a3..5f0c9f6c9e 100644
--- a/hermes_cli/models.py
+++ b/hermes_cli/models.py
@@ -301,6 +301,20 @@ _PROVIDER_MODELS: dict[str, list[str]] = {
     "copilot-acp": [
         "copilot-acp",
     ],
+    "antigravity-acp": [
+        "gemini-3.7-flash-high",
+        "gemini-3.7-flash-medium",
+        "gemini-3.7-flash-low",
+        "gemini-3.6-flash-high",
+        "gemini-3.6-flash-medium",
+        "gemini-3.6-flash-low",
+        "gemini-3-flash-agent",
+        "gemini-3.5-flash-low",
+        "gemini-3.5-flash-extra-low",
+        "gemini-pro-agent",
+        "gemini-3.1-pro-low",
+        "antigravity-acp",
+    ],
     "copilot": [
         "gpt-5.4",
         "gpt-5.4-mini",
@@ -1130,6 +1144,7 @@ CANONICAL_PROVIDERS: list[ProviderEntry] = [
     ProviderEntry("nvidia",         "NVIDIA NIM",               "NVIDIA NIM (Nemotron models via build.nvidia.com or local NIM)"),
     ProviderEntry("copilot",        "GitHub Copilot",           "GitHub Copilot (Uses GITHUB_TOKEN or gh auth token)"),
     ProviderEntry("copilot-acp",    "GitHub Copilot ACP",       "GitHub Copilot ACP (Spawns copilot --acp --stdio)"),
+    ProviderEntry("antigravity-acp", "Google Antigravity ACP",   "Google Antigravity ACP (Spawns agy_acp_server.par ACP kernel)"),
     ProviderEntry("huggingface",    "Hugging Face",             "Hugging Face Inference Providers"),
     ProviderEntry("gemini",         "Google AI Studio",         "Google AI Studio (Native Gemini API)"),
     ProviderEntry("vertex",         "Google Vertex AI",         "Google Vertex AI (Gemini via GCP; OAuth2 service account or ADC, GCP billing/quotas)"),
@@ -1293,6 +1308,10 @@ _PROVIDER_ALIASES = {
     "github-model": "copilot",
     "github-copilot-acp": "copilot-acp",
     "copilot-acp-agent": "copilot-acp",
+    "antigravity": "antigravity-acp",
+    "antigravity-acp": "antigravity-acp",
+    "agy-acp": "antigravity-acp",
+    "antigravity-acp-agent": "antigravity-acp",
     "google": "gemini",
     "google-gemini": "gemini",
     "google-ai-studio": "gemini",
diff --git a/hermes_cli/providers.py b/hermes_cli/providers.py
index ab47df203f..e2592665d4 100644
--- a/hermes_cli/providers.py
+++ b/hermes_cli/providers.py
@@ -94,6 +94,12 @@ HERMES_OVERLAYS: Dict[str, HermesOverlay] = {
         base_url_override="acp://copilot",
         base_url_env_var="COPILOT_ACP_BASE_URL",
     ),
+    "antigravity-acp": HermesOverlay(
+        transport="codex_responses",
+        auth_type="external_process",
+        base_url_override="acp://antigravity",
+        base_url_env_var="ANTIGRAVITY_ACP_BASE_URL",
+    ),
     "github-copilot": HermesOverlay(
         transport="openai_chat",
         extra_env_vars=("COPILOT_GITHUB_TOKEN", "GH_TOKEN"),
diff --git a/hermes_cli/runtime_provider.py b/hermes_cli/runtime_provider.py
index bc8b25924b..63f209a783 100644
--- a/hermes_cli/runtime_provider.py
+++ b/hermes_cli/runtime_provider.py
@@ -2025,10 +2025,15 @@ def resolve_runtime_provider(
                 "requested_provider": requested_provider,
             }
 
-    if provider == "copilot-acp":
+    # Local ACP subprocess providers (GitHub Copilot CLI, Google
+    # Antigravity ACP kernel, …) — resolved generically by auth_type.
+    _ext_proc_config = PROVIDER_REGISTRY.get(provider)
+    if provider == "copilot-acp" or (
+        _ext_proc_config and _ext_proc_config.auth_type == "external_process"
+    ):
         creds = resolve_external_process_provider_credentials(provider)
         return {
-            "provider": "copilot-acp",
+            "provider": provider,
             "api_mode": "chat_completions",
             "base_url": creds.get("base_url", "").rstrip("/"),
             "api_key": creds.get("api_key", ""),
diff --git a/hermes_cli/setup.py b/hermes_cli/setup.py
index d18c813f5e..b8f7419cc0 100644
--- a/hermes_cli/setup.py
+++ b/hermes_cli/setup.py
@@ -74,6 +74,20 @@ _DEFAULT_PROVIDER_MODELS = {
     "copilot-acp": [
         "copilot-acp",
     ],
+    "antigravity-acp": [
+        "gemini-3.7-flash-high",
+        "gemini-3.7-flash-medium",
+        "gemini-3.7-flash-low",
+        "gemini-3.6-flash-high",
+        "gemini-3.6-flash-medium",
+        "gemini-3.6-flash-low",
+        "gemini-3-flash-agent",
+        "gemini-3.5-flash-low",
+        "gemini-3.5-flash-extra-low",
+        "gemini-pro-agent",
+        "gemini-3.1-pro-low",
+        "antigravity-acp",
+    ],
     "copilot": [
         "gpt-5.4",
         "gpt-5.4-mini",
diff --git a/hermes_cli/web_server.py b/hermes_cli/web_server.py
index 5e378bdda7..541bdb44a9 100644
--- a/hermes_cli/web_server.py
+++ b/hermes_cli/web_server.py
@@ -9788,9 +9788,26 @@ def _copilot_acp_status() -> Dict[str, Any]:
         "logged_in": False,
         "source": "copilot_cli",
         "source_label": "Managed by the GitHub Copilot CLI",
-        "token_preview": None,
-        "expires_at": None,
-        "has_refresh_token": False,
+        "cli_command": "copilot /login",
+        "docs_url": "https://docs.github.com/en/copilot",
+        "status_fn": None,
+    }
+
+
+def _antigravity_acp_status() -> Dict[str, Any]:
+    """Status for antigravity-acp — credentials are owned by the ACP kernel.
+
+    Same conservative read-only card as copilot-acp: the Antigravity ACP
+    kernel (agy_acp_server.par) manages its own OAuth state under
+    ~/.gemini/antigravity-acp/.
+    """
+    return {
+        "logged_in": False,
+        "source": "antigravity_acp_kernel",
+        "source_label": "Managed by the Antigravity ACP kernel",
+        "cli_command": "hermes auth status antigravity-acp",
+        "docs_url": "https://antigravity.google",
+        "status_fn": None,
     }
 
 
@@ -9864,6 +9881,14 @@ _OAUTH_PROVIDER_CATALOG: tuple[Dict[str, Any], ...] = (
         "docs_url": "https://docs.github.com/en/copilot",
         "status_fn": _copilot_acp_status,
     },
+    {
+        "id": "antigravity-acp",
+        "name": "Google Antigravity (ACP)",
+        "flow": "external",
+        "cli_command": "hermes auth status antigravity-acp",
+        "docs_url": "https://antigravity.google",
+        "status_fn": _antigravity_acp_status,
+    },
     # ── Anthropic / Claude entries sit at the bottom: the API-key path
     # first, then the subscription OAuth path (which only works with extra
     # usage credits on top of a Claude Max plan — see disclaimer in name).
diff --git a/tools/delegate_tool.py b/tools/delegate_tool.py
index e4f4766fca..031afa4501 100644
--- a/tools/delegate_tool.py
+++ b/tools/delegate_tool.py
@@ -1540,9 +1540,12 @@ def _build_child_agent(
         effective_acp_args = []
 
     if override_acp_command:
-        # If explicitly forcing an ACP transport override, the provider MUST be copilot-acp
-        # so run_agent.py initializes the CopilotACPClient.
-        effective_provider = "copilot-acp"
+        # If explicitly forcing an ACP transport override, the provider MUST be
+        # an ACP-backed one (copilot-acp or antigravity-acp) so run_agent.py
+        # initializes the ACP client. Default to copilot-acp as before when the
+        # effective provider is not already ACP-backed.
+        if effective_provider not in {"copilot-acp", "antigravity-acp"}:
+            effective_provider = "copilot-acp"
         effective_api_mode = "chat_completions"
 
     # Resolve reasoning config: delegation override > parent inherit
